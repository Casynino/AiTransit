node_modules
.next
out
build
.DS_Store
*.pem

# env
.env
.env*.local

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# typescript
*.tsbuildinfo
next-env.d.ts

# vercel
.vercel

# Locally-stored uploads. The local disk driver is a laptop convenience —
# production uses Vercel Blob — so customer receipts and cargo photos must
# never ride into the repository.
/public/uploads/

# local editor / tooling config
.claude/
.env*
!.env.example

# Manual screenshots are rebuilt from the running app by scripts/manuals/capture.mjs
docs/manuals/shots/
backups/

# The local dev database lives in ~/.aitransit-pg, outside the repo — but if
# somebody points AITRANSIT_PGDATA at a directory in here, do not commit it.
/.aitransit-pg/
